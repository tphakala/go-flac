# Adapted from github.com/tphakala/birdnet-go's .golangci.yaml to keep one
# linting standard across projects. go-flac-specific adaptations:
#   - dropped the frontend/ exclusion (no frontend here)
#   - dropped the birdnet-go-specific forbidigo logger patterns and the gosec
#     settings block (no structured logger; gosec not enabled)
#   - kept gocritic's ruleguard wired to rules/*.go for modern-Go patterns
#   - modernize stays disabled: it panics on Go 1.26 + atomic types in
#     golangci-lint v2.11.4 (known bug)
version: "2"
output:
  sort-order:
    - linter
    - severity
    - file
linters:
  enable:
    - bodyclose
    - copyloopvar
    - dupl
    - durationcheck
    - errname
    - errorlint
    - exhaustive
    - fatcontext
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - iface
    - ineffassign
    - misspell
    - nilerr
    - nilnil
    - predeclared
    - prealloc
    - revive
    - staticcheck
    - testifylint
    - thelper
    - unconvert
    - wastedassign
  disable:
    - unused
  settings:
    gocognit:
      min-complexity: 50
    gocritic:
      # gocritic auto-enables the ruleguard check when settings.ruleguard.rules
      # is set; the rules/*.go matchers carry the //go:build ruleguard tag so
      # the normal toolchain ignores them.
      disabled-checks:
        - commentFormatting
        - commentedOutCode
      enabled-tags:
        - style
        - diagnostic
        - performance
      settings:
        hugeParam:
          # pcm.Config is a public config struct passed by value throughout the
          # encoder API (NewEncoder, EncodeInterleaved, NewFrameEncoder); adding the
          # Vorbis-comment fields pushed it to 96 bytes, just over gocritic's default
          # 80-byte threshold. Raise the bound so the deliberate by-value config does
          # not trip the check, while genuinely large params (>= 128 bytes) still do.
          sizeThreshold: 128
        ruleguard:
          rules: "rules/*.go"
    revive:
      rules:
        - name: unused-parameter
          disabled: true
    prealloc:
      simple: true
      range-loops: true
      for-loops: true
    exhaustive:
      default-signifies-exhaustive: true
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  uniq-by-line: true
  new: false
